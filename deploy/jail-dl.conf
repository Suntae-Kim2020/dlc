# /etc/fail2ban/jail.d/dl.conf
[dl]
enabled  = true
filter   = dl
logpath  = /var/log/caddy/dl.log
backend  = auto
port     = http,https
# 10분 안에 10번 실패하면 1시간 차단.
# 관리자 비밀번호 하나로 쓰기 기능 전체가 열리는 구조라, 앱 자체에는 시도
# 횟수 제한이 없다. 여기가 유일한 무차별 대입 방어선이다.
findtime = 600
maxretry = 10
bantime  = 3600
